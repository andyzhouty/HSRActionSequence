# 后续评估项

本文件只保留当前重构后仍值得持续评估、但不影响本轮方案验收的事项。已完成的身份注册、保存数据迁移、技能解析、生命周期、端口抽象、测试分层和旧入口清理不再重复记录。

## 1. 模拟器状态的进一步收敛

`src/simulate/types.ts` 已将通用调度状态、领域状态和角色机制状态分组，但部分高耦合角色仍通过同一个机制状态对象共享字段。后续可按召唤物、领域、SP 角色和资源机制继续拆成可辨识的状态命名空间，并逐步消除剩余的扁平可选字段。

## 2. 角色规则的精确 schema

角色数据的 CID、名称、速度、技能键、技能表达式和跨角色 `targetCid` 已有运行时校验；`effectRules` 的具体规则字段仍保留开放扩展结构。后续可为领域、忆灵、追击和速度规则分别建立精确 schema，并从 schema 生成 TypeScript 类型。

## 3. 机制注册表的自动发现

当前机制注册表已经统一了记录行动后的钩子，但注册项仍集中维护在 `src/simulate/mechanics/registry.ts`。角色数量继续增加时，可改为按机制能力导出清单并由构建期校验注册，避免注册表重新变成新的大型总分发入口。

## 4. 生成绑定的中文注释来源

`frontend/wailsjs` 的方法集合已由 `check:bindings` 校验，目录仍由 Wails 生成。若后续需要持续维护中文注释，应优先在生成模板或生成后脚本中处理，避免重新生成时覆盖手工修改。

## 5. 工具脚本的类型检查

数据、文档、架构和绑定脚本已经纳入 `pnpm check`，Go 入口也有 `go test`、`go vet` 和格式检查。若脚本数量继续增加，可单独引入 `scripts/tsconfig.json` 或将脚本迁移为 TypeScript，进一步减少脚本运行时类型错误。

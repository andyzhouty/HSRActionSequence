# 排轴器架构重构实施方案

## 1. 目标与约束

本方案用于把当前代码审查中值得改进的部分，拆成可以独立实施、独立验证和必要时独立回滚的工作项。总体目标是让角色机制、模拟器、保存数据、前端状态和测试体系拥有清晰边界，同时保持现有排轴结果和用户操作不变。

### 总体目标

- 统一角色身份、CID、名称和技能标识的来源，消除字符串散落和同一角色多套标识并存的问题。
- 将技能字符串、保存数据和角色配置从“宽泛字符串/可选字段”逐步收敛为可校验的领域模型。
- 明确模拟器的行动阶段和状态边界，使机制执行顺序可读、可测、可扩展。
- 分离行动意图、模拟结果、展示模型和持久化数据，减少 `GeneratedAction` 与 `ActionState` 的职责膨胀。
- 拆分前端巨型 Context 和页面编排逻辑，降低角色专属状态对全局状态的污染。
- 让 Node 模拟器测试、jsdom UI 测试、集成测试、质量测试各司其职，并在 CI 中执行同一套质量门禁。

### 必须遵守的约束

- 每个阶段开始前保留可通过测试的基线；重构阶段不得顺便修改无关机制数值。
- 先增加类型、适配器和测试，再迁移调用方，最后删除兼容代码。
- 任何保存数据结构变化都必须提供版本号、迁移函数和旧数据测试。
- 角色机制只能通过统一入口接入模拟器；禁止新代码继续向总分发 `switch` 添加无边界分支。
- 生成目录中的文件不作为长期手工维护的业务逻辑；需要改变生成结果时，优先修改生成源或生成流程。
- 每个提交只解决一个可描述的问题，提交前运行与改动范围匹配的测试，阶段结束再运行完整检查。

## 2. 当前基线

重构开始前记录以下基线；如果后续结果发生变化，必须明确是预期行为变化还是回归。

- 当前测试基线：51 个测试文件、492 个测试通过。
- `pnpm exec tsc --noEmit` 通过。
- `pnpm run build` 通过；构建的大体积 chunk 警告单独评估，不作为本轮重构阻断项。
- `pnpm run check:docs` 通过。
- 测试目录已按 `unit`、`integration`、`ui`、`quality`、`fixtures` 初步分类，后续只做职责收敛，不再按角色随意复制测试入口。
- 当前工作区存在将英文自然语言注释改为中文的未提交改动；实施重构时不得覆盖或回退这些改动。

每个阶段完成后至少执行：

```text
pnpm test -- --run
pnpm exec tsc --noEmit
pnpm run build
pnpm run check:docs
git diff --check
```

如果阶段涉及格式、脚本或 CI，再追加 `pnpm run lint`、`pnpm run format:check` 和对应脚本检查。

## 3. 分阶段实施路线

### 阶段 0：冻结基线并建立重构护栏

#### 目标

在改动架构前固定“结果不能悄悄变化”的判断标准，并补齐后续迁移所需的自动检查。

#### 主要工作

- 建立代表性排轴场景快照，至少覆盖：普通角色行动、召唤物行动、领域/特殊行动、SP Blade、Archer、Cyrene、Hyacine、Phainon，以及包含多角色联动的队伍。
- 将模拟器输出中真正属于业务结果的字段提取为稳定快照，避免快照绑定无关的 React 或展示字段。
- 为 `getCharacterCid`、角色名规范化、技能规则读取增加边界测试：未知 CID、重复 CID、别名、缺失规则和大小写差异。
- 增加“数据校验失败即阻断”的脚本入口，先覆盖角色 CID 唯一性、角色必填字段、技能键、速度格式和跨角色引用。
- 记录 Node、jsdom、构建、文档检查的执行命令，作为后续 CI 矩阵的唯一来源。

#### 交付物

- 可重复的模拟结果快照/契约测试。
- 角色数据和保存数据的基础校验命令。
- 重构前后都能执行的基线检查说明。

#### 验收标准

- 同一输入在本地连续执行得到相同的核心行动序列和资源结果。
- 注入未知角色 CID 或非法技能键时，校验命令能给出具体文件和字段。
- 基线测试在没有业务代码变化的情况下稳定通过。

#### 回滚边界

本阶段只新增测试和检查脚本，不改变运行时行为；若快照设计不合理，删除新增快照即可回滚，不影响角色机制。

### 阶段 1：建立领域基础模型和数据边界

#### 目标

统一角色身份和技能标识，并为角色配置、保存数据和行动数据建立运行时可校验的模型。

#### 主要工作

- 新建集中管理角色身份的模块，至少定义 `CharacterId`、规范名称、别名和角色元数据；禁止业务代码直接重复写 CID 字符串。
- 将当前 `CharacterEntry` 中的 `Record<string, string>`、`Record<string, unknown>` 按用途拆分为明确类型。暂时无法完全类型化的字段，集中放入带校验的边界适配器。
- 将 `SkillCode = string` 改造成可解析的技能标识/技能 AST。第一版只需覆盖现有技能表达式，并明确无法解析时的错误信息。
- 为角色 JSON、技能规则和保存数据增加 schema 校验；类型推导和运行时校验使用同一份定义，避免 TypeScript 类型与实际 JSON 脱节。
- 为 `SavedData` 增加明确版本字段、默认值归一化函数和迁移入口。所有导入、读取本地数据和初始化默认值必须经过同一入口。
- 已处理 `src/utils/actionSequence.ts` 对 `src/utils/action-sequence` 的兼容导出：所有使用方已统一到规范路径，旧入口已删除。
- 对 Cyrene 等跨角色目标引用使用 `CharacterId` 校验，不再依赖任意字符串。

#### 建议涉及目录

```text
src/domain/identity/
src/domain/skills/
src/domain/saved-data/
src/data/
src/utils/action-sequence/
scripts/
tests/unit/data/
tests/unit/domain/
```

#### 验收标准

- 所有角色 CID 唯一，所有跨角色引用可在校验阶段解析。
- 非法 JSON、未知技能、缺少版本号或过期保存数据不会直接进入模拟器，而是得到可定位的错误或经过迁移。
- 现有合法保存数据迁移后，排轴结果与迁移前一致。
- 新增角色不需要在多个工具函数中重复登记名称、CID 和别名。

#### 回滚边界

保留旧数据读取适配器和旧技能字符串入口至少一个完整版本周期。若新模型阻塞某角色，可暂时由适配器转回旧结构，但不得绕过边界校验。

### 阶段 2：重构模拟器状态与行动生命周期

#### 目标

把隐含在 `loop.ts`、`normal-action/index.ts` 和各类 handler 中的行动阶段显式化，并拆分角色机制状态与通用模拟状态。

#### 主要工作

- 定义明确的行动生命周期，例如：选取行动者、行动前检查、行动意图生成、目标解析、伤害/资源结算、召唤物或领域后处理、行动记录提交。
- 将 `loop.ts` 的顺序改为调用生命周期阶段，而不是继续扩大一个总分发函数。
- 将 `ActionState` 拆成稳定的通用状态和按领域划分的机制状态，例如角色资源、召唤物、领域、特殊角色机制；角色专属状态通过明确的扩展对象或注册表挂载。
- 为行动排序定义统一比较器，明确 AV 精度、同 AV 时的次序、浮点误差和行动 ID；禁止各 handler 自行比较浮点数。
- 为循环保护、最大行动数、资源不变量和状态转移增加统一错误/诊断信息；避免只用一个魔数 `guard < 2000` 隐式兜底。
- 将角色机制从 `normal-action/index.ts` 的大型分支逐步迁移到统一的机制注册接口。优先迁移低耦合角色，再迁移召唤物、领域和高耦合 SP 角色。
- 为每次迁移保留旧入口与新入口对照测试，确认行动序列、资源变化和机制触发次数一致。

#### 建议迁移顺序

1. 普通角色和不含召唤物的技能。
2. Archer、Cyrene、Hyacine、Phainon 等已整理过文件边界的角色。
3. 召唤物、领域和行动前/行动后全局效果。
4. SP Blade、SP Robin、SP Aventurine 等具有跨角色状态或特殊行动的角色。

#### 验收标准

- 生命周期阶段在类型和代码结构中可直接看出，新增阶段不需要修改多个无关 handler。
- `ActionState` 不再包含大量与当前模拟无关的可选角色字段；角色状态拥有明确归属。
- 同 AV 行动在不同运行次数中顺序稳定，超过最大行动数或检测到非法状态时能报告原因。
- 全部机制迁移后，阶段 0 的排轴快照无未解释差异。

#### 回滚边界

按角色或机制组迁移，每组保留旧 handler 的兼容分支和对照测试。某一组出现差异时，只回退该组注册，不回退已经稳定的生命周期基础设施。

### 阶段 3：分离行动意图、模拟结果、展示和持久化

#### 目标

解决 `GeneratedAction` 同时承担模拟输入、展示信息、编辑信息和保存格式的问题。

#### 主要工作

- 定义四类边界对象：

  - `ActionIntent`：模拟器要执行的行动意图。
  - `ActionResult`：结算后的状态变化、触发事件和诊断信息。
  - `ActionViewModel`：表格、时间线和导出所需的展示字段。
  - `PersistedAction`：保存数据中允许长期稳定的字段。

- 建立从模拟结果到展示模型、从用户编辑到行动意图的单向转换函数。
- 把 `GeneratedAction` 中的角色机制标志、UI 选择标志、导出字段和模拟字段分开；不能立即迁移的字段先放在兼容层，不继续增加可选字段。
- 将导出 Excel、图片导出、表格编辑和模拟器分别依赖各自的模型，不直接共享整个 `ActionState`。
- 统一速度、持续时间、伤害和 AV 的数值表示，明确展示精度与计算精度的转换位置。

#### 验收标准

- 模拟器核心可以在没有 React、Wails 和导出组件的环境下运行。
- 修改表格显示字段不会改变模拟结果；修改模拟结果不会要求 UI 状态携带角色专属内部字段。
- 保存数据只包含经过版本化定义的稳定字段，导入旧数据仍可迁移。
- 导出测试只验证导出契约，不依赖模拟器内部实现。

#### 回滚边界

转换层可以保留旧 `GeneratedAction` 作为输入/输出兼容格式。新模型只在一条功能链路启用，通过配置或适配器切换，确认无差异后再扩大范围。

### 阶段 4：拆分前端全局状态和页面编排

#### 目标

降低 `ActionSequenceContext` 和 `ActionSequence.tsx` 的职责密度，使状态更新路径可追踪、可测试。

#### 主要工作

- 按职责拆分 Context 或 reducer：队伍/角色配置、模拟选项、资源与限制、行动序列编辑、菜单/选择 UI、保存/导入导出。
- 将跨组件业务操作收敛为 command/action 和 selector，组件不直接组合大量互相依赖的 setter。
- 把默认值、保存数据加载、迁移和重置逻辑移到独立 controller/hook；页面只负责组合布局和路由级生命周期。
- 将角色专属菜单改为声明式注册：角色提供菜单能力和动作描述，通用 action panel 负责渲染和派发。
- 识别仅供单个组件使用的状态并下沉，避免所有状态都挂在全局 Context 中造成无关重渲染。
- 为导入、重置、切换队伍、编辑行动等关键操作增加 reducer/selector 测试。

#### 建议涉及目录

```text
src/contexts/
src/pages/action-sequence/
src/components/action-panel/
src/components/action-table/
src/hooks/
tests/unit/ui-state/
tests/integration/ui/
```

#### 验收标准

- 任一 UI 操作可以定位到单一 command/reducer，再定位到对应领域转换函数。
- Context 不再暴露与页面无关的全部内部 setter；组件不依赖角色模拟器的完整状态对象。
- 导入/重置/切换队伍和行动编辑的现有交互测试全部通过。
- 角色专属菜单新增项只需注册能力，不需要修改通用菜单的大型条件分支。

#### 回滚边界

先在内部引入新 reducer/selector，保留旧 Context facade。逐个迁移组件，迁移完成后再删除旧字段，避免一次性改动整个页面树。

### 阶段 5：整理后端边界、生成代码和数据工具链

#### 目标

使前端不直接绑定 Wails 具体实现，并让生成文件、数据校验和文档检查有明确来源。

#### 主要工作

- 为后端调用定义最小端口接口，例如文件读写、导出、系统能力；Wails 实现放在 adapter，测试使用 fake/in-memory 实现。
- 统一错误、取消、超时和不可用环境的处理，不让组件直接判断 Wails 运行时细节。
- 记录 `frontend/wailsjs` 的生成源、生成命令和允许修改范围；如果可以自动生成，则在 CI 检查生成结果是否干净。
- 扩展 `scripts/checkDocPaths.mjs` 或新增数据检查脚本，覆盖角色、技能、CID、路径、schema、生成绑定和保存数据迁移。
- 将 `tsconfig`、lint、格式化、脚本和 Go/Wails 检查纳入统一 `pnpm run check`，避免本地通过但 CI 漏检。

#### 验收标准

- UI 单元测试不需要启动 Wails，也不需要修改全局运行时对象。
- 生成绑定可以按文档重新生成，并能检测手工漂移。
- 非法角色数据、断开的文档路径和失配的生成文件在提交前失败。

#### 回滚边界

保留现有 Wails 调用作为 adapter 实现；接口化只改变依赖方向，不立即改变导出业务。生成流程确认稳定后再限制手工编辑。

### 阶段 6：重构测试分类并与 CI 对齐

#### 目标

让测试目录、运行环境和测试目标一一对应，补足架构迁移期间最需要的契约测试。

#### 主要工作

- 将纯模拟器、领域模型、数据校验和迁移测试放入 Node 环境的 `tests/unit` 或 `tests/mechanics`；不要让纯逻辑测试依赖全局 jsdom。
- 将组件交互、Context/reducer 和导出 UI 测试放入 jsdom 的 `tests/ui`；仅通过公开交互验证行为。
- 将完整队伍、多角色联动和导入到渲染的链路放入 `tests/integration`；避免在 unit 和 integration 重复验证同一内部函数。
- 将类型、schema、CID 唯一性、随机/属性测试和性能测试放入 `tests/quality`，并提供单独、稳定的命令。
- 为跨层接口增加契约测试：角色注册表、技能解析器、保存数据迁移、模拟结果到展示模型、Wails port。
- 用参数化测试覆盖角色矩阵，公共行为使用 fixture/helper；角色特殊规则保留独立测试，避免为了复用而隐藏断言。
- 将性能测试的输入、随机种子、时间预算和机器差异写清楚；性能测试不作为普通 PR 的不稳定阻断项时，改为定期任务或有宽容阈值的质量检查。
- 重新审视 `vitest.config.ts` 的全局 jsdom、并行和 retry 设置，按项目或目录选择环境，不以 retry 掩盖 flaky 测试。

#### 验收标准

- `pnpm test:unit` 不加载 DOM；UI 测试显式使用 jsdom；集成测试不重复承担所有单元断言。
- 目录名称、脚本名称和 CI 任务名称一致，开发者可单独运行失败层级。
- 关键契约在角色增加或数据迁移时能尽早失败。
- 删除或重命名测试文件后，不会出现同一测试被多个入口重复执行。

### 阶段 7：清理兼容层并固化架构规则

#### 目标

在迁移稳定后删除临时入口，防止系统重新退化为字符串分发、巨型状态和跨层依赖。

#### 主要工作

- 删除旧 `GeneratedAction`/`ActionState` 兼容字段、旧技能字符串入口和已迁移的角色分支。
- 删除只为过渡存在的 re-export、facade、双写逻辑和旧保存数据临时路径；保留正式迁移器。
- 增加依赖方向检查：数据层不依赖 UI，模拟器不依赖 React/Wails，展示层不反向修改领域状态。
- 对角色机制注册接口、保存数据版本和技能 AST 写开发文档与新增角色模板。
- 更新根目录文档、测试说明和贡献流程，明确新增角色、技能、测试和数据迁移的标准步骤。

#### 验收标准

- 全部正式代码通过依赖边界检查，兼容代码数量可通过搜索确认已清零或有明确保留理由。
- 新增一个简单角色只需要添加领域定义、机制实现、数据和对应测试，不需要修改多个总入口。
- 从旧版本保存数据升级到当前版本的迁移测试完整通过。

## 4. 工作项拆分与依赖关系

建议按以下顺序建立提交或合并请求，每项完成后保持主分支可构建：

| 编号 | 工作项 | 依赖 | 主要验收 |
| --- | --- | --- | --- |
| R0 | 基线快照、数据检查、命令清单 | 无 | 基线可重复，非法数据可定位 |
| R1 | 角色身份/CID 注册表 | R0 | 无重复 CID，跨角色引用可解析 |
| R2 | 保存数据版本、默认值和迁移 | R0 | 旧数据迁移结果一致 |
| R3 | 技能标识解析器和规则 schema | R1 | 合法技能可解析，非法技能明确失败 |
| R4 | 模拟器生命周期接口 | R1、R3 | 生命周期顺序可测试 |
| R5 | 通用状态与角色机制状态拆分 | R4 | 代表性角色结果无差异 |
| R6 | 行动意图/结果/展示/持久化模型 | R2、R4 | 模拟器与 UI/导出解耦 |
| R7 | 角色机制注册表和分批迁移 | R4、R5、R6 | 总分发分支持续减少 |
| R8 | 前端 reducer、selector 和 Context 拆分 | R2、R6 | 关键 UI 操作可追踪 |
| R9 | Wails port、生成代码和工具链 | R6、R8 | UI 测试脱离 Wails，生成结果可校验 |
| R10 | 测试环境、契约测试和 CI 矩阵 | R0，可与 R4-R9 并行 | CI 与本地检查一致 |
| R11 | 删除兼容层、依赖检查和文档固化 | R1-R10 | 新增功能遵守边界 |

R1、R2、R3 可以在 R0 完成后并行；R4 之前必须完成至少 R1 和 R3。R8 不应早于 R6，否则前端会继续绑定尚未稳定的行动结构。R11 必须等所有迁移和 CI 门禁完成后执行。

## 5. 风险、判断标准与处理方式

### 角色机制结果发生变化

优先使用阶段 0 的核心结果快照和旧/新入口对照测试定位差异。若差异是修复已确认的错误，单独提交并注明旧行为、新行为和影响范围；无法证明是修复时，回退当前角色迁移，不修改快照掩盖问题。

### 保存数据无法兼容

任何 schema 变更先增加旧版本 fixture，再实现迁移。迁移必须幂等：同一份数据重复迁移不会继续改变结果。无法恢复的数据只允许明确报错，不能静默丢弃角色、行动或资源字段。

### 类型模型过早过严

先在输入边界完成 `unknown -> validated domain type`，内部保持强类型；对于确实开放的扩展字段使用显式 `ExtensionData`，不把 `Record<string, unknown>` 重新扩散到核心模块。

### 机制注册表成为新的总分发

注册表只负责能力发现和生命周期钩子，不负责承载所有角色条件判断。角色条件、资源结算和行动生成应留在角色/领域机制模块；若注册表需要大量角色专属字段，暂停迁移并重新切分接口。

### 测试并行或 retry 掩盖不稳定

先用固定种子、独立 fixture 和明确清理找出共享状态，再调整并行度。只有确认是环境限制时才降低并行；不得把提高 retry 次数作为稳定性方案。

### 前端拆分导致改动面过大

优先新增 facade 和 selector，逐组件迁移。任何阶段都保留可运行的旧入口，只有新旧路径的行为测试一致后才删除旧 API。

## 6. 完成定义

本方案全部完成应同时满足：

- 全量测试、类型检查、构建、文档路径检查、格式和 lint 检查均通过。
- 角色 CID、技能和保存数据均经过运行时校验，旧数据迁移有覆盖测试。
- 模拟器生命周期显式，行动排序和循环保护有统一定义。
- 角色机制不再依赖不断扩大的总分发分支，通用状态不再承载大量角色专属可选字段。
- 行动模拟、展示、编辑、导出和持久化使用明确的转换边界。
- 前端 Context 和页面职责已拆分，UI 测试不依赖 Wails 实现。
- 本地检查命令与 CI 检查矩阵一致，生成代码和数据脚本的来源有文档。
- 所有兼容层都有删除时间点；若仍需保留，必须在代码和文档中写明原因、影响及后续处理人。

## 7. 首批实施顺序（已执行）

本轮已按以下顺序完成基础模型、边界适配器和端到端迁移，之后再扩展到全量机制：

1. 完成 R0：建立代表性排轴快照、角色/保存数据校验入口和稳定的 Node/jsdom 检查命令。
2. 完成 R1-R3：建立唯一角色身份注册表、保存数据迁移入口、技能解析器和数据 schema 校验。
3. 完成 R4-R7：显式化行动生命周期、统一排序与循环保护、拆分行动模型，并通过机制注册表迁移角色机制。
4. 完成 R8-R9：拆分前端状态 Provider、建立 selector/command 边界、抽象 Wails port 并校验生成绑定。
5. 完成 R10-R11：按 Node/jsdom/质量/性能职责执行测试，统一 CI 门禁，清理旧入口并增加依赖边界检查。

本轮最终验证命令为 `pnpm run check`、`pnpm run build`、`pnpm run test:quality`、`pnpm run test:performance` 和 `git diff --check`；性能测试保持独立，不纳入普通 PR 门禁。

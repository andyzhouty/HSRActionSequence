# 真珠行动轴机制实现方案

## 目标

为真珠（CID 1503）加入最小可用的行动轴机制。当前仓库尚未收录真珠的角色数据或运行时代码，本方案按新增角色处理。

仅实现：

- 真珠的基础身份、欢愉命途和基础速度。
- 终结技选择一名除自身外的我方角色作为【美学底本】。
- 按我方欢愉角色数量，使【美学底本】行动提前 10%/15%/30%。
- 欢愉角色达到 4 名时，使【美学底本】获得 1 个额外回合。
- 星魂 2 使除真珠和【美学底本】外的其他我方欢愉角色同样行动提前。

不实现伤害、治疗、资源、防御、能量、强化普攻、欢愉技、秘技、抗性穿透及其他不改变行动顺序的效果。

## 实施方案

1. 在 `src/data/characters.json` 增加 CID 1503，只填写 `cid`、`names`、`path`、`baseSpeed`。
2. 在 `src/data/characterMechanics.json` 增加真珠的终结技效果标识和参演编号 `participantId: 104`；不添加与伤害或资源相关的字段。
3. 新增 `src/mechanics/pearl.ts`，集中处理：
   - 欢愉角色数量对应的行动提前比例；
   - 终结技目标校验，拒绝自身、敌方和无效目标；
   - 4 名欢愉角色时的标准额外回合；
   - 星魂 2 的其他欢愉角色行动提前，并避免对【美学底本】重复处理。
4. 将真珠终结技接入现有终结技后处理入口，复用 `advanceNotPastCurrent` 和项目已有的额外回合记录机制，不新增独立生命周期；Q 按项目现有规则通过 `AQ/EQ` 触发。
5. 统计对象限定为我方实际角色，不将敌人、忆灵或其他非角色实体计入欢愉角色数量；具体判断复用现有身份和命途工具。

## 测试

新增真珠机制单元测试，覆盖：

- 1/2/3 名欢愉角色时分别行动提前 10%/15%/30%；
- 4 名及以上时恰好生成 1 个额外回合；
- 无效目标和自身目标不产生效果；
- 星魂 2 只额外影响其他欢愉角色，不重复影响真珠和【美学底本】；
- 终结技正常施放与被插队施放使用相同结果；
- 不生成伤害、治疗、资源或强化普攻相关结果。

先运行针对性测试，再运行 `pnpm run check:ci`，确认数据校验、类型检查和现有行动轴测试均通过。

## 验收标准

- `pearl.md` 中列出的每一项行动轴效果都有对应实现或测试。
- 真珠的普攻、战技及其他非行动轴效果不会进入模拟器逻辑。
- 行动提前不越过当前行动，额外回合使用现有排序和展示规则。
- 不影响其他角色、忆灵和终结技的既有行动顺序。

# 衣匠行动并入普通忆灵流程的改造方案

## 1. 结论

衣匠目前被单列为一个行动生命周期阶段，主要是现有实现为了隔离阿格莱雅专属状态而采取的工程性做法，并不是因为衣匠在行动类型上不同于普通忆灵。

衣匠应当像其他普通忆灵一样进入普通行动流程：由调度器选中后，使用忆灵自身固定的技能生成行动，执行通用行动前后处理，再执行阿格莱雅专属的层数、速度和至高之姿联动。

本次改造的目标是移除 `garmentmakerPhase.ts` 和生命周期中的 `garmentmakerAction` 阶段，但不删除以下仍然属于阿格莱雅机制的内容：

- E、Q 和秘技召唤衣匠。
- 衣匠层数累计、层数上限和层数带来的速度变化。
- 阿格莱雅至高之姿期间的速度联动。
- 至高之姿倒计时、重新施放 Q、衣匠离场和状态重置。
- 衣匠的唯一行动键、行动编号和行动值覆盖行为。

## 2. 当前为什么需要单列

当前代码把衣匠视为一种特殊状态，而不是普通忆灵状态，相关逻辑分散在以下位置：

- `src/mechanics/aglaea.ts`
  - 使用 `isGarmentmakerState` 和 `garmentmakerOwnerId` 标识衣匠。
  - 维护层数、生成代次、基础速度、当前速度和阿格莱雅状态同步。
  - `handleGarmentmakerAction` 直接创建行动、增加层数并安排下一动。
- `src/simulate/scheduler.ts`
  - 为衣匠单独生成带生成代次的行动键。
- `src/simulate/lifecycle.ts`
  - 在普通行动之前增加 `garmentmakerAction` 生命周期阶段。
- `src/simulate/garmentmakerPhase.ts`
  - 单独处理行动前插队、衣匠行动、迷迷提前行动、行动后插队和长夜自毁。
- `src/utils/action-sequence/types.ts` 及表格组件
  - 使用 `isAglaeaGarmentmakerAction` 作为展示和编辑判断条件。

之所以最初需要这样做，主要有以下几个原因。

### 2.1 普通技能解析不适用于衣匠

普通行动会根据角色、行动编号和保存数据读取技能覆盖。衣匠没有用户可编辑的技能选择，技能应由阿格莱雅机制规则固定提供。如果直接进入普通行动流程，必须先避免把衣匠当成可输入技能的角色处理。

### 2.2 衣匠的行动会修改主人状态

衣匠行动不是单纯生成一条行动记录，还会增加层数、重算衣匠速度，并同步刷新阿格莱雅至高之姿速度。原实现把这些状态修改和行动生成写在同一个专用函数中，避免普通行动流程遗漏或错误处理。

### 2.3 衣匠使用了特殊的召唤代次和行动键

衣匠可能被重新召唤。生成代次用于保证新旧行动键不冲突，也用于使旧的行动值覆盖不会错误作用于新一代衣匠。这个需求被放进调度器的特殊分支后，暂时与普通忆灵的键生成逻辑分离。

### 2.4 专用阶段暂时承担了通用收尾工作

`garmentmakerPhase.ts` 不只处理阿格莱雅机制，还重复处理了插队、迷迷提前行动和长夜自毁等生命周期工作。因为专用阶段直接返回，衣匠不会完整经过 `handleNormalAction` 和 `runPostActionCleanup`。

这能避免当时的普通流程误读衣匠技能，但也造成了行为不一致：同样是忆灵行动，衣匠和其他忆灵经过的通用处理链不同，未来新增通用忆灵效果时容易漏掉衣匠。

## 3. 改造后的目标结构

### 3.1 区分“普通行动流程”和“角色专属机制”

普通行动流程负责所有实体都可能需要的生命周期工作：

1. 读取行动者和行动上下文。
2. 处理行动前插队。
3. 解析本次行动的技能。
4. 生成统一格式的行动记录。
5. 执行通用行动后效果。
6. 计算下一次行动值并递增行动编号。
7. 执行统一收尾和回调。

阿格莱雅机制只负责衣匠专属行为：

- 生成衣匠时构造固定技能和主人关系。
- 在衣匠行动生成后增加层数。
- 根据层数更新衣匠及阿格莱雅速度。
- 处理至高之姿的召唤、倒计时和离场。

也就是说，衣匠的特殊性应表现为普通流程中的一个机制钩子，而不是一条独立生命周期。

### 3.2 建立忆灵行动配置

在模拟器内部增加一个轻量的行动配置解析层，例如 `resolveActionProfile`。它需要根据当前状态返回：

- 技能标识。
- 是否锁定技能输入。
- 是否为忆灵行动。
- 忆灵主人 ID。
- 是否需要执行角色技能解析和角色专属后效。

至少支持两类配置：

- 普通角色：沿用现有 `skillOverrides`、终结技兼容字段和角色技能规则。
- 普通忆灵：使用召唤规则提供的固定技能，忽略角色技能覆盖，并自动锁定技能输入。

衣匠应通过该配置层提供自己的固定技能，而不是由生命周期阶段直接拼接完整 `GeneratedAction`。

### 3.3 统一运行时的忆灵归属信息

当前 `isGarmentmakerState`、`isMemeState`、`isEveyAction`、`isPolluxAction` 等字段的含义并不完全一致。此次不要求一次性重构全部忆灵，但衣匠合并时应先明确以下通用信息：

- 当前状态是否为忆灵。
- 忆灵的主人 ID。
- 忆灵的固定技能。
- 忆灵的生成代次。
- 忆灵是否允许普通行动流程处理。

第一步可以保留旧字段作为兼容标记；完成迁移后，再将衣匠的通用识别改为 `character.kind === "忆灵"` 加主人关系和行动配置，避免 UI、调度器和模拟器继续依赖多个衣匠专属布尔字段。

## 4. 具体实施步骤

### 阶段一：建立基线和行为契约

在修改生命周期前，为当前实现补充或整理阿格莱雅测试，记录以下结果：

- E、Q、AQ、QE、QA 和插队 Q 的行动顺序。
- 秘技在行动值 0 的召唤和顶轴行动。
- 衣匠每次行动的技能、行动编号、速度、主人 ID 和行动键。
- 层数增加、层数上限和 E4 行为。
- 衣匠速度变化对下一动行动值的影响。
- 星期日直接选择衣匠与选择阿格莱雅时的拉条差异。
- 至高之姿倒计时正常触发、手动提前、手动延后和 Q 重置。
- 倒计时结束后衣匠离场且不会继续产生行动。
- 有迷迷、长夜、晴空乐手、死龙或其他忆灵的混合队伍。
- 衣匠行动前后配置终结技插队时的顺序。

测试应优先断言核心行动序列和状态结果，不把 `isAglaeaGarmentmakerAction` 作为唯一判断依据。该字段后续可能被删除或降级为展示兼容字段。

### 阶段二：把衣匠固定技能接入普通行动解析

新增普通行动配置解析函数，并在 `handleNormalAction` 开始位置计算：

- `isMemospriteTurn`。
- `resolvedSkill`。
- `lockedSkill`。
- `memospriteOwnerId`。

衣匠行动应得到类似以下语义：

```text
技能 = 衣匠规则中的普通攻击技能
技能输入 = 锁定
终结技 = 否
角色技能覆盖 = 不读取
行动类型 = 忆灵
主人 = 阿格莱雅
```

普通流程中的角色专属逻辑必须以角色实体为前提。衣匠不得触发角色本人的 Archer、Cyrene、Firefly、The Herta、Saber 等技能分支，也不得被当成可施放 Q 的角色。

### 阶段三：将衣匠行动生成并入普通主行动

把当前 `handleGarmentmakerAction` 拆成两个职责：

- 通用部分：由 `handleNormalAction` 生成行动记录、填入忆灵类型、主人 ID、固定技能、速度和锁定状态。
- 阿格莱雅部分：新增类似 `handleAglaeaMemospriteAction` 的机制钩子，在行动记录生成后增加层数并刷新相关速度。

普通行动流程需要保留以下顺序：

1. 处理行动前插队。
2. 生成衣匠行动。
3. 结算需要按行动计数的通用效果。
4. 执行阿格莱雅层数和速度钩子。
5. 安排衣匠下一动。
6. 处理行动后插队、迷迷提前行动、长夜自毁及其他统一收尾。

衣匠不应进入角色技能的 Q 后处理、角色召唤处理和角色专属技能输入处理。可以通过行动配置中的能力标记或明确的 `isCharacterTarget` 判断实现，而不是再次新增一个生命周期分支。

### 阶段四：合并通用收尾并删除专用生命周期

将 `garmentmakerPhase.ts` 中的通用行为迁移到已有普通流程：

- 行动前终结技插队 → 普通行动的行动前插队。
- 迷迷提前行动 → `runPostActionCleanup` 中已有的统一处理。
- 行动后终结技插队 → 普通行动的行动后插队。
- 长夜自毁回调 → 普通行动的统一收尾。
- Saber、Mydei、Evanescia 等跨角色收尾 → 按普通行动的现有顺序执行，并用测试确认不会产生额外触发。

迁移后：

- 从 `ACTION_LIFECYCLE_PHASES` 删除 `garmentmakerAction`。
- 从 `runActionLifecycle` 删除 `handleGarmentmakerActionTurn` 调用。
- 删除 `src/simulate/garmentmakerPhase.ts`。
- `specialAction` 继续保留给倒计时、阿哈、SP Robin 晴空乐手、死龙、长夜等仍然不是普通行动的实体。

### 阶段五：整理调度器和行动展示模型

调度器仍需保证衣匠重新召唤时行动键唯一，但不应通过“衣匠是特殊生命周期”来实现。建议将生成代次抽象成普通召唤物通用字段，由调度器统一生成：

```text
实体 ID + 生成代次 + 行动编号
```

需要同步检查：

- 旧行动值覆盖不会作用于新一代衣匠。
- 普通行动记录中的 `isMemospriteAction`、`memospriteOwnerId`、`lockedSkill` 正确填充。
- 表格展示、目标选择、技能输入和行动排序优先使用通用忆灵字段。
- `isAglaeaGarmentmakerAction` 只在确有阿格莱雅专属展示需求时保留；否则删除并迁移对应测试。
- 既有 `actionDisplayOrder.ts` 不再因为衣匠的特殊生命周期而单独分支。

### 阶段六：清理阿格莱雅专属代码边界

完成普通流程迁移后，整理 `src/mechanics/aglaea.ts`：

- 保留召唤、层数、速度、至高之姿和倒计时逻辑。
- 将 `handleGarmentmakerAction` 改为只处理阿格莱雅机制后效，或删除并由普通行动钩子直接调用层数更新函数。
- 避免 `AglaeaActionState` 继续作为整个模拟器基础状态的隐式来源；逐步把阿格莱雅字段归入明确的机制状态组。
- 让倒计时继续使用独立特殊实体，因为倒计时不是忆灵普通回合，且其行动会移除衣匠并结束至高之姿。

## 5. 测试改造方案

### 必须保留的回归测试

- `tests/unit/mechanics/aglaea.test.ts` 中的速度、召唤、至高之姿、倒计时和拉条测试全部保留。
- 现有测试从 `isAglaeaGarmentmakerAction` 逐步改为同时验证通用字段：`isMemospriteAction`、`memospriteOwnerId`、技能和行动值。
- 对行动键的测试保留，因为它直接关系到手动行动值覆盖和重新召唤。

### 必须新增的普通流程测试

- 衣匠经过 `handleNormalAction`，不再经过独立生命周期阶段。
- 衣匠技能固定且锁定，输入中的同名 `skillOverrides` 不会改变衣匠技能。
- 衣匠不会触发角色专属的 Q 后效、角色技能解析或角色召唤逻辑。
- 衣匠会执行应当适用于忆灵的通用行动前后处理，并且每一项只执行一次。
- 衣匠行动后层数、衣匠速度、阿格莱雅至高之姿速度和下一行动值与旧实现一致。
- 插队 Q、迷迷提前行动、长夜自毁和跨角色追加行动不会因为生命周期合并而重复或丢失。
- 衣匠与其他忆灵同时行动时，排序和同 AV 优先级稳定。

### 迁移对照测试

在删除旧阶段前，可以临时保留旧入口，用相同输入分别运行旧流程和新流程，对比：

- 行动键和行动顺序。
- 行动值、速度和行动编号。
- 角色和忆灵状态的层数、速度、下一行动值。
- 至高之姿倒计时和衣匠离场结果。
- 通用回调产生的额外行动数量。

对照通过后删除旧入口，不保留双路径运行时分支。

## 6. 不在本次范围内的内容

- 不把阿格莱雅至高之姿倒计时合并为普通忆灵行动；它仍是独立的倒计时实体。
- 不在本次同时重构死龙、长夜、晴空乐手等其他特殊忆灵，以免把多个行为差异混在同一迁移中。
- 不改变游戏机制数值、速度公式、层数上限、召唤时机和 Q 行为。
- 不因为追求字段统一而立即删除所有忆灵专属标记；先迁移衣匠，再根据实际重复模式决定是否抽象其他忆灵。

## 7. 验收标准

- `ACTION_LIFECYCLE_PHASES` 中不再存在 `garmentmakerAction`。
- `src/simulate/garmentmakerPhase.ts` 被删除。
- 衣匠由普通行动流程生成，且使用统一的忆灵行动字段。
- 衣匠技能不可被角色技能覆盖修改。
- 阿格莱雅层数、速度、至高之姿、倒计时和重新召唤行为与迁移前一致。
- 通用行动前后处理只执行一次，没有遗漏或重复的插队、拉条和自毁回调。
- 阿格莱雅相关单元测试、模拟器 Node 测试、UI 测试和全量检查全部通过。
- 代码中不再存在只为衣匠单列生命周期而保留的分支；剩余衣匠判断必须能说明其对应的是阿格莱雅机制、展示需求或召唤物身份。

## 8. 风险与回滚边界

### 风险

- 普通行动流程可能让衣匠意外读取技能覆盖或触发角色专属后效。
- 统一收尾可能导致插队、拉条、Saber、Mydei 或长夜回调重复执行。
- 状态数组中删除或重建衣匠时，`stateIndex` 变化可能影响当前行动者。
- 普通忆灵字段和衣匠兼容字段同时存在时，UI 可能出现展示分支不一致。

### 控制方式

- 先建立旧/新结果对照，再删除旧阶段。
- 将固定技能解析和角色技能解析分成两个明确分支。
- 所有可能修改 `states` 的机制都通过当前状态 ID 重新定位，不依赖失效的数组下标。
- 每次迁移只修改行动生命周期和阿格莱雅调用边界，不顺便修改机制数值。

### 回滚边界

阶段一至阶段三可以只增加配置层、钩子和对照测试，不删除旧阶段。若普通流程出现差异，可恢复 `garmentmakerPhase.ts` 作为临时入口。

只有在回归测试和对照测试全部通过后，才删除生命周期阶段、旧处理函数和兼容字段。
